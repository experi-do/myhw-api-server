apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: sk023-myfirst-ingress
  namespace: skala-practice
spec:
  ingressClassName: public-nginx
  rules:
  - host: sk023-ingress.skala25a.project.skala-ai.com
    http:
      paths:
      - backend:
          service:
            name: sk023-myhw-api-server
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - 'sk023-ingress.skala25a.project.skala-ai.com'
    secretName: sk023-ingress-project-tls-cert
